<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <Description>Shared logic for ConfigTransform: layer parsing, case-insensitive file resolution, and layer-resolution reporting. Format-agnostic — consumed by ConfigTransform.Xml, ConfigTransform.Json, and ConfigTransform.Cli.</Description>
  </PropertyGroup>

</Project>
