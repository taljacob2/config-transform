<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <Description>XDT-based layered merge engine for App.config, Web.config, and other XML config files — the XML half of ConfigTransform.Cli's format registry. See CONFIG_MANAGEMENT.md §5.2 and §6.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Web.Xdt" Version="3.1.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ConfigTransform.Core\ConfigTransform.Core.csproj" />
  </ItemGroup>

</Project>
