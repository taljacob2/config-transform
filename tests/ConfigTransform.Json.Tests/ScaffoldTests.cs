using Xunit;

namespace ConfigTransform.Json.Tests;

public class ScaffoldTests
{
    // Placeholder — real merge-correctness tests against the DotNetCore and GenericJson
    // fixture sets (including the documented JSON-array merge behavior) are implemented in a
    // later slice. See CONFIGTRANSFORM_TOOL_DESIGN.md §3.2.
    [Fact]
    public void Scaffold_compiles_and_runs()
    {
        Assert.True(true);
    }
}
